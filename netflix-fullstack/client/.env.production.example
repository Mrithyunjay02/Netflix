# ─── PRODUCTION ENVIRONMENT ──────────────────────────────────────────────────
# Set this in Netlify → Site Settings → Environment Variables
# Do NOT commit a real backend URL here — use .env.production.local instead
#
# Example:
# VITE_API_URL=https://your-backend.onrender.com/api
#
# The URL must:
#  - Point to your live Express backend (NOT localhost)
#  - End with /api  (e.g. https://myapp.onrender.com/api)
#  - Include HTTPS
VITE_API_URL=https://your-backend-url.com/api

# TMDB key is the same in prod — you can leave this or override per environment
VITE_TMDB_API_KEY=5df25401a52fe108cdb8571818926ded
